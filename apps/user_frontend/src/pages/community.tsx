import { Container } from "@mui/material";
import React from "react";

type Props = {};

export default function Community({}: Props) {
  return <Container sx={{ mt: 4 }}>Community</Container>;
}

Community.getLayout = true;
